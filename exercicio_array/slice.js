let frutas = ["morango", "uva", "maça"];
let variavel = frutas.slice()[0];
console.log(variavel)

//Faça um programa que tenha um  ARRAY, contendo 3 elementos, e ultilizando o slice, pegue a primeira letra de cada elemento.
let cores = ["azul", "verde", "amarelo"];
let cor_um_primeira_letra = cores[0];
let cor_dois_primeira_letra = cores[1];
let cor_tres_primeira_letra = cores[2];
console.log(`Primeira letra da palavra ${cores[0]}: ${cor_um_primeira_letra.slice()[0]}`);
console.log(`Primeira letra da palavra ${cores[0]}: ${cor_dois_primeira_letra.slice()[0]}`);
console.log(`Primeira letra da palavra ${cores[0]}: ${cor_tres_primeira_letra.slice()[0]}`);